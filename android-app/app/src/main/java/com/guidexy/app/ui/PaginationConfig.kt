package com.guidexy.app.ui

object PaginationConfig {
    const val resultsPerPage = 20 // Arama ekranı liste sonuçları
    const val reviewsPerPage = 20 // Yorumlar sekmesi sayfalama
    const val categoriesPerPage = 12 // Kategori listesi sayfalama
    const val citiesPerPage = 12 // Şehir listesi sayfalama

    const val homeLatestCount = 10 // Ana sayfa - son eklenen işletmeler
    const val homePopularCount = 10 // Ana sayfa - popüler işletmeler
    const val homeMostViewedCount = 10 // Ana sayfa - en çok görüntülenen işletmeler
    const val homeFavoritesCount = 10 // Ana sayfa - en çok favoriye eklenen işletmeler
    const val homeRecentReviewsCount = 10 // Ana sayfa - son yapılan yorumlar

    const val listLatestMaxItems = 1000 // Son eklenen işletmeler sayfası toplam limiti
    const val listLatestPerPage = 20 // Son eklenen işletmeler sayfası sayfa başı
    const val listPopularMaxItems = 1000 // Popüler işletmeler sayfası toplam limiti
    const val listPopularPerPage = 20 // Popüler işletmeler sayfası sayfa başı
    const val listMostViewedMaxItems = 1000 // En çok görüntülenenler sayfası toplam limiti
    const val listMostViewedPerPage = 20 // En çok görüntülenenler sayfası sayfa başı
    const val listFavoritesMaxItems = 1000 // Favoriler sayfası toplam limiti
    const val listFavoritesPerPage = 20 // Favoriler sayfası sayfa başı
    const val recentReviewsMaxItems = 1000 // Son yorumlar sayfasında toplam gösterilecek yorum limiti
    const val recentReviewsPerPage = 20 // Son yorumlar sayfasında sayfa başına kaç yorum gösterileceği
}
