# Task-21: API v1 Standardizasyonu (Mobil Hazır)

## Tamamlananlar

- `/api/v1/...` endpoint seti aktif edildi.
- Legacy `/api/...` route seti korunarak backward compatibility sağlandı.
- Frontend `apiBase` konfigürasyonu `/api/v1` olacak şekilde güncellendi.
- Mobil client'lar için versiyonlu API kullanım modeli netleştirildi.
